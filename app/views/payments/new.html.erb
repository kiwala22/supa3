<%= provide(:title, "New Payments") %>
<h3>
   <%= "Payments" %>
   <small><%= "Create New Payments" %></small>
</h3>
<div class="col-lg-offset-3 col-lg-6">
   <div class="panel panel-dark panel-flat">
      <div class="panel-body">
         <p class="text-muted pv rtp_red">UPLOAD PAYMENT LIST IN ONLY XLSX FORMAT.</p>
         <%= form_with(url: {action: :create}, multipart: true) do |f| %>
         <div class="form-group">
            <%= f.file_field 'list', required: true %>
         </div>
         <div class="form-group">
            <%= f.submit "Upload", class: "btn btn-primary start" %>
         </div>
         <% end %>

      </div>
   </div>
</div>
