<%= provide(:title, "Merchant Push Requests") %>
<h3>
   <%= "Push Requests" %>
   <small><%= "All Push Requests" %></small>
</h3>

<div class="panel panel-default">
   <%= search_form_for @q, url: push_pay_broadcasts_path, html: { method: :get } do |f| %>
   <div class="panel-body">
      <fieldset>
         <div class="form-group">
            <%= f.label :phone_number, class:"col-lg-2 control-label" %>
            <div class="col-sm-6">
               <%= f.search_field :phone_number_eq, { include_blank: true, :class=>"form-control", id: "phone_number"}%>
            </div>
         </div>
      </fieldset>
      <div class="pannel-footer">
         <div class="clearfix">
            <div class="pull-right">
               <%= f.submit 'Search', :class=>'btn btn-primary' %>
            </div>
         </div>
      </div>
   </div>
   <% end %>
</div>

<div class="table-responsive">
   <table class="table table-striped table-bordered table-hoverlarge-12 columns large-centered">
      <thead>
         <tr>
            <th>No</th>
            <th>Phone Number</th>
            <th>Amount</th>
            <th>Status</th>
            <th>Time</th>
         </tr>
      </thead>
      <tbody>
         <% @push_pay_broadcasts.each_with_index do |push_pay_broadcasts, i| %>
         <tr>
            <td><%= (i+1) %></td>
            <td><%= payment.phone_number %></td>
            <td><%= payment.amount %></td>
            <td><%= payment.status %></td>
            <td><%= (payment.created_at.to_datetime.strftime("%b %d, %Y %I:%M%P")) %></td>
         </tr>
         <% end %>
      </tbody>
   </table>
   <%= paginate @push_pay_broadcasts %>

</div>
