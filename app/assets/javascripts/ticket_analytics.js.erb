// require 'walden'
var myChart = echarts.init(document.getElementById('ticket_analytics'), 'walden');
var option = {
    title: {
      text: 'Ticket Analytics',
        subtext: 'Distribution of Tickets'
    },
    tooltip : {
        trigger: 'axis',
        axisPointer : {            // 坐标轴指示器，坐标轴触发有效
            type : 'shadow'        // 默认为直线，可选为：'line' | 'shadow'
        }
    },
    legend: {
        data:['Tickets']
    },
    grid: {
        left: '5%',
        right: '5%',
        bottom: '5%',
        containLabel: true
    },
    xAxis : [
        {
            type : 'category',
            data : gon.labels
        }
    ],
    yAxis : [
        {
            type : 'value'
        }
    ],
    series : [
        {
            name:'Tickets',
            type:'bar',
            barWidth : 12,
            data: gon.tickets
        }
    ]
};

// use configuration item and data specified to show chart
myChart.setOption(option);
